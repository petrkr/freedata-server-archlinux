# FreeDATA Server Arch linux package

This server package of FreeDATA.

This package can run as standalone or you can install also freedata-gui to install WebGUI which will be running as part of server.
